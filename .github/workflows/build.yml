name: Build installers

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifact: ScubedTimesheet.exe
            install_browser: false      # uses system Edge at runtime, no install needed
          - os: macos-latest
            artifact: ScubedTimesheet
            install_browser: true
            playwright_browser: chromium
          - os: ubuntu-latest
            artifact: ScubedTimesheet
            install_browser: true
            playwright_browser: chromium

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt pyinstaller

      - name: Install Playwright browser
        if: matrix.install_browser
        run: python -m playwright install ${{ matrix.playwright_browser }}

      - name: Build
        run: pyinstaller scubed_timesheet.spec

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ScubedTimesheet-${{ matrix.os }}
          path: dist/${{ matrix.artifact }}

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/${{ matrix.artifact }}
